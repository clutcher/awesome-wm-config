# Install cli packages for comfort work
sudo apt install mc jq zip\
 curl wget \
 git git-lfs\
 imagemagick openvpn

# Install libraries
sudo apt install libglib2.0-bin

# Install drivers
sudo apt install firmware-linux

# Hardware support
sudo apt install network-manager bluez pipewire-audio alsa-utils